# %posttrans for open-sesame (headless)
# Runs after ALL packages in the transaction complete.
# Two-phase restart: mark units, then file triggers call enqueue-marked.

sesame_require_systemd

if [ -x /usr/lib/systemd/systemd-update-helper ]; then
    /usr/lib/systemd/systemd-update-helper mark-restart-user-units \
        open-sesame-profile.service \
        open-sesame-secrets.service \
        open-sesame-launcher.service \
        open-sesame-snippets.service \
        || :
fi

:
