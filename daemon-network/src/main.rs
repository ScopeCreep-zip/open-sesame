//! daemon-network: Open Sesame network transport daemon.
//!
//! Provides Noise XX encrypted peer sessions over UDP/TCP with TOFU
//! identity pinning, rate limiting, and stateless cookie `DoS` resistance.
//!
//! Stub — Milestone 1 implements the full daemon.

fn main() {
    eprintln!("daemon-network: not yet implemented (requires Milestone 1)");
    std::process::exit(1);
}
